# Only unrelated files

The single file in this directory, so that a test can point `--data` at a directory holding nothing
a walk recognises as data. The run evaluates nothing, and has to say so rather than report a silent
PASS. Do not delete, and do not add a .yaml, .yml, .json, .jsn or .template file here.
