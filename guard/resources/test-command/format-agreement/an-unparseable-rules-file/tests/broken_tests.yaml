# Present so the rules file is not skipped for having no tests. Its expectation is never reached: the
# rules file does not parse, so no rule is ever evaluated against this input.
- name: Never evaluated, because the rules file does not parse
  input:
    Resources:
      ExampleS3:
        Type: AWS::S3::Bucket
  expectations:
    rules:
      a: PASS
