{
  "name": "cfn-guard",
  "author": "Amazon Web Services",
  "version": "0.0.1",
  "description": "Typescript module for cfn-guard",
  "main": "ts-lib/index.js",
  "types": "ts-lib/index.d.ts",
  "files": [
    "ts-lib/**/*.js",
    "ts-lib/**/*.d.ts",
    "ts-lib/**/*.js",
    "ts-lib/**/*.d.ts",
    "ts-lib/**/*.wasm",
    "ts-lib/**/*.wasm.d.ts"
  ],
  "keywords": [],
  "license": "MIT",
  "scripts": {
    "build:wasm-pack": "wasm-pack build --target nodejs --out-name guard --out-dir ./ts-lib",
    "build:typescript": "tsc -b",
    "build": "npm run build:wasm-pack && npm run build:typescript && rm ./ts-lib/.gitignore && rm ./ts-lib/README.md",
    "package": "npm pack",
    "test": "jest",
    "test:update": "jest --u"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/lodash": "^4.17.1",
    "@types/node": "^20.12.7",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.1.3",
    "wasm-pack": "0.12.0"
  },
  "dependencies": {
    "lodash": "^4.17.21"
  }
}
