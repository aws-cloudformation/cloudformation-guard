let excluded_resources = [
    /AWS::AmazonBroker/,
    /AWS::App*/
]

let resources = Resources.*[
    Type !in %excluded_resources
]

rule assert_all_resources_have_non_empty_tags when %resources !empty {
    %resources.Properties.Tags !empty
}
