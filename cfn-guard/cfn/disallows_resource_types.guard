let disallowed_resource_types = [
    /AWS::IAM/,
    /AWS::S3/,
    /AWS::EC2::VPC/
]

rule disallowed_resource_types {
    Resources.*.Type IN %disallowed_resource_types
}

rule only_allowed_resource_types {
    Resources.*.Type NOT IN %disallowed_resource_types
}
