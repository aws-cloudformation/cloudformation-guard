AWS::EC2::Volume Encrypted == true
AWS::EC2::Volume Tags.*.Key == %compliant_tag_name
AWS::Lambda::Function Tracing IN %tracing_config
let compliant_tag_name = uai
let tracing_config = [Active,PassThrough]
